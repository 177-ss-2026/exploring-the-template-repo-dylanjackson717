1. package.json is like a manifest or shopping list for the project. While node_modules is the actual folder where the physical code for those libraries are stored.
2. It allows you to embed JavaScript variables or expressions directly into a string.
3.  After i saved it it, it basically just cleaned up my code the moment I saved.
4.  I made 4 commits. We added them because it s easier to debug, creates a paper trail, and its reversibility.
5.  const = default. let = when you know the value must change. var = old way of doing things (we typically avoid using it)